{
  "name": "block3",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E for TodoMVC (block3)",
  "type": "commonjs",
  "scripts": {
    "test:e2e": "playwright test --output=test-results",
    "test:e2e:ui": "playwright test --ui --output=test-results"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
